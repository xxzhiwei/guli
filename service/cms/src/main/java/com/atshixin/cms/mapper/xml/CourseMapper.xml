<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atshixin.cms.mapper.CourseMapper">
    <select id="getCourses" resultType="com.atshixin.cms.vo.CourseListItem">
        SELECT
               ec.*
        FROM edu_course ec
                    LEFT JOIN edu_teacher et ON ec.teacher_id = et.id
                    LEFT JOIN edu_subject es ON ec.subject_parent_id = es.id
                    LEFT JOIN edu_subject es2 ON ec.subject_id = es2.id ${ew.customSqlSegment}
    </select>
</mapper>
